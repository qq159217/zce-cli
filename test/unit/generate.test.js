const path = require('path')
const assert = require('assert')
const generate = require('../../lib/generate')

describe('lib/generate', () => {
  describe('#generate1', () => {
    it('should generate https://github.com/zce-templates/demo', done => {

    })
  })
})
